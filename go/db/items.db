CREATE TABLE categories(id INTEGER, name TEXT NOT NULL, PRIMARY KEY(id));
CREATE TABLE items(id INTEGER NOT NULL, name TEXT, category_id INT, image_name TEXT, PRIMARY KEY(id), FOREIGN KEY (category_id) REFERENCES categories(id));
